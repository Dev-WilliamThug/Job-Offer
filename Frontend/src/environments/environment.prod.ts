/**
 * Environnement de production (Vercel).
 *
 * ⚠️ Remplacez ces URLs par celles de votre backend Django déployé
 *    (Render, Railway, PythonAnywhere, etc.) avant le déploiement final.
 */
export const environment = {
  production: true,
  apiUrl: 'https://VOTRE-BACKEND.onrender.com/api',
  mediaUrl: 'https://VOTRE-BACKEND.onrender.com',
};
